<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.hmw.trekit.SplashActivity"
    android:background="@drawable/b_g_gradient">

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/splash_icon_image"
        android:background="@drawable/icon"
        android:layout_centerHorizontal="true"
        android:layout_above="@+id/splash_map_image"
        android:layout_marginBottom="159dp"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/splash_title_text"
        android:text="@string/splash_title"
        android:textColor="@android:color/white"
        android:textSize="40sp"
        android:layout_centerHorizontal="true"
        android:layout_above="@+id/splash_map_image"
        android:layout_marginBottom="114dp"/>

    <ImageView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/map"
        android:id="@+id/splash_map_image"
        android:scaleType="fitCenter"
        android:layout_alignParentBottom="true"/>
</RelativeLayout>
